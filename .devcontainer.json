{
    "build": { "dockerfile": "Dockerfile" },

    "customizations": {
        "vscode": {
          "extensions": ["teros-technology.teroshdl",
            "ms-vscode.cpptools",
            "ms-vscode.makefile-tools"]
        }
      },

    "runArgs": ["--privileged"],

    "mounts": ["type=bind,source=/dev/bus/usb,target=/dev/bus/usb"]

}